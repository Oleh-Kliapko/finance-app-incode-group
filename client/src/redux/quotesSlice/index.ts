import { quotesReducer, addQuotes } from "./quotesSlice";
import {
  selectLastQuotes,
  selectHistoryQuotes,
  selectQuotesByTicker,
} from "./selectors";

export {
  quotesReducer,
  addQuotes,
  selectLastQuotes,
  selectHistoryQuotes,
  selectQuotesByTicker,
};
